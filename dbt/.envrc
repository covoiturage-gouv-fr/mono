source_up_if_exists
unset VIRTUAL_ENV
if [ ! -x .venv/bin/python ] || [ requirements.txt -nt .venv/.deps-installed ]; then
  rm -rf .venv
  uv venv --no-project --quiet
  uv pip install --no-deps --quiet -r requirements.txt
  touch .venv/.deps-installed
fi
source .venv/bin/activate
